# 🇵🇭 Cebu IT Park カフェ検索アプリ 説明書

## 📋 目次
1. [サイト概要](#サイト概要)
2. [主な機能](#主な機能)
3. [ユーザー向けガイド](#ユーザー向けガイド)
4. [管理者向けガイド](#管理者向けガイド)
5. [技術スタック](#技術スタック)
6. [セットアップガイド](#セットアップガイド)
7. [デプロイメント](#デプロイメント)

---

## サイト概要

### 🎯 何ができるアプリ？

**Cebu IT Park カフェ検索アプリ**は、フィリピン・セブシティのIT Park周辺のカフェ情報を集約した、デジタルノマド・英語学習者・リモートワーカー向けの専門アプリです。

- 🌍 対象地域：セブIT Park 周辺
- 📱 言語対応：日本語・英語（自動切り替え）
- 🎨 デザイン：ダークテーマ（ダークガラスモーフィズム）
- 🚀 技術：React + Laravel + Leaflet

### 💼 想定ユーザー

- デジタルノマド
- ESL学生（英語学習者）
- リモートワーカー
- フリーランス
- 旅行者

---

## 主な機能

### 🎯 1. スマートカフェ検索

#### 📍 リスト表示
- すべてのカフェを表形式で一覧表示
- 名前、営業時間、WiFi、電源、予算で検索・フィルター
- ワンクリックで詳細情報を表示

#### 🗺 マップ表示
- インタラクティブな地図（Leaflet + CartoDB）
- カフェの正確な位置を表示
- ドットをクリックすると詳細ポップアップ表示

### 📶 2. WiFi・電源スペック

すべてのカフェに以下の情報を記載：

| WiFi | 説明 | 電源 | 説明 |
|------|------|------|------|
| **EXCELLENT** ⭐ | 100Mbps以上（超高速） | **YES** ✅ | コンセント完備 |
| **GOOD** 🟢 | 50-100Mbps（高速） | **LIMITED** ⚠️ | 部分的に利用可 |
| **AVERAGE** 🟡 | 50Mbps未満（普通） | **NO** ❌ | コンセント無し |

### 🕐 3. 営業時間・24時間対応

- 営業時間を詳細に表示
- 24時間営業カフェには🌙アイコン付き
- 現在時刻に基づいて「営業中」「閉店中」を表示

### 💰 4. 予算表示

- 1杯のコーヒーの平均価格を表示
- ₱（フィリピンペソ）で表示
- IT Park周辺は₱120～₱250が目安

### 🏷️ 5. タグシステム

各カフェに複数のタグを付与：

- #作業向き - 落ち着いた環境で作業に適している
- #会話OK - 友人とおしゃべりしても大丈夫
- #24時間 - 24時間営業
- #大人数OK - グループで利用可能

### 💬 6. レビューシステム

全ユーザーがレビューを投稿可能：

**レビュー内容**：
- WiFi速度の評価（5段階★）
- 電源の充実度（5段階★）
- 静粛性（5段階★）
- コスパ（5段階★）
- 雰囲気（5段階★）
- 混雑度（空いてる / ふつう / 混んでる）
- ユーザータグ（複数選択可）
- コメント（自由記入、任意）

**レビュー機能**：
- ✍️ 新規投稿
- ✏️ 自分の投稿を編集
- 🗑 自分の投稿を削除
- ⭕ ソフトデリート対応（管理者側で復元可能）

### 📸 7. 画像アップロード

- ドラッグ&ドロップまたはファイル選択で複数画像を追加
- JPEG / PNG / WebP 対応
- 1ファイル最大3MB制限
- 実際のカフェ内の雰囲気が分かる写真が重要

### ☕ 8. カフェ診断機能（クイズ）

「今日どこ行く？診断」：
- 3つの質問に答えるだけ
- AIロジックで最適なカフェを提案
- 気になったカフェはすぐに詳細表示

### ♡ 9. マイリスト機能

- 気になったカフェを「保存」できる
- 保存済みカフェだけをフィルター表示
- ローカルストレージに保存されるため、登録不要

### 🌙 / ☀️ 10. ダーク/ライトテーム切り替え

- デフォルト：ダークテーマ
- ワンクリックでテーマ切り替え可能
- 設定はローカルストレージに保存

---

## ユーザー向けガイド

### 使い方（初心者向け）

#### 1️⃣ 最初のステップ

1. サイトを開く → http://127.0.0.1:8000
2. 言語を選択（右上の国旗アイコンまたは自動検出）
3. カフェ一覧が表示される

#### 2️⃣ カフェを探す

**方法1：検索**
- 上部の検索ボックスにカフェ名やキーワードを入力
- リアルタイムで結果が絞り込まれる

**方法2：フィルター**
- WiFi（EXCELLENT / GOOD / AVERAGE）
- 電源（YES / LIMITED / NO）
- 営業中のみ
- 保存済みのみ

**方法3：マップから探す**
- 「マップ」タブに切り替え
- ドットをクリックしてカフェ詳細を表示

#### 3️⃣ カフェの詳細を見る

詳細ページに表示される情報：
- 📍 Google Maps へのリンク
- 📶 WiFi スピード
- 🔌 電源状況
- 🕐 営業時間
- 💰 予算
- 📸 店内の写真（複数）
- 📝 ユーザーレビュー
- 🏷️ タグ

#### 4️⃣ レビューを投稿

1. 詳細ページの「レビューを投稿」セクションまでスクロール
2. 各項目を評価（★5段階）
3. 混雑度を選択
4. タグを選択（複数可）
5. コメントを記入（任意）
6. 「投稿する」をクリック

#### 5️⃣ マイリストに保存

1. カフェ詳細ページの「♡ マイリストに保存」ボタンをクリック
2. ハート♥が表示されたら保存完了
3. 「保存済みのみ」フィルターで確認可能

#### 6️⃣ カフェ診断を使う

1. 「☕ 今日どこ行く？診断」ボタンをクリック
2. 3つの質問に答える
3. 最適なカフェが提案される
4. 「このカフェを見る」で詳細を確認

---

## 管理者向けガイド

### ログイン方法

1. http://127.0.0.1:8000/admin/login にアクセス
2. 管理者認証情報でログイン
   - セットアップ時に指定したメールアドレスとパスワードを使用
   - ⚠️ 本番環境では強力なパスワードを使用してください

### 📊 ダッシュボード

管理者ログイン後、以下の統計情報が表示：
- ☕ 登録カフェ総数
- 🌙 24時間営業カフェ数
- 💰 平均予算
- 📶 WiFi レベル分布
- 🔌 電源状況分布
- 🏷️ タグ別カフェ数
- 🕐 最近追加されたカフェ

### ☕ カフェ管理

#### 1. カフェ一覧の表示

- すべてのカフェを表形式で表示
- 名前、営業時間、WiFi、電源、予算、タグが表示
- 件数が表示される

#### 2. カフェ新規追加

「＋ 新規カフェを追加」をクリック：

**基本情報**：
- カフェ名（必須）
- Vibe/雰囲気（日本語・英語別記入）
- 説明文（日本語・英語別記入）

**営業情報**：
- 営業時間（例：7:00 – 21:00）
- 24時間営業チェックボックス
- WiFi レベル（EXCELLENT / GOOD / AVERAGE）
- 電源状況（YES / LIMITED / NO）
- 予算（例：₱200〜）

**詳細スペック**：
- メニュー情報（日本語・英語）
- 座席情報（日本語・英語）
- 騒音レベル（日本語・英語）
- おすすめポイント（日本語・英語）
- コツ・注意点（日本語・英語）

**地図情報**：
- Google Maps URL
- 緯度・経度（セブ市内に限定）

**その他**：
- 写真（最大3枚、JPEG/PNG/WebP、最大3MB）
- タグ（カンマ区切り、例：#作業向き,#24時間）
- アクセントカラー（HEXコード、例：#2ABFBF）

#### 3. カフェ情報の編集

カフェ一覧から「✏️ 編集」をクリック：
- 上記のすべての情報を編集可能
- 画像の追加・削除も可能
- 「保存」をクリックして更新

#### 4. カフェの削除

カフェ一覧から「🗑 削除」をクリック：
- 確認ダイアログが表示される
- 「削除する」で完全に削除（復元不可）

### 💬 クチコミ管理

#### 1. レビュー一覧

すべてのユーザーレビューを一覧表示：
- カフェ名
- ユーザー名
- 投稿日時
- 評価サマリー

#### 2. レビューの編集

レビューの詳細を確認・編集：
- 各評価値を変更可能
- コメントを編集可能

#### 3. レビューの削除

不適切なレビューを削除：
- 通常削除：完全に削除
- ソフトデリート対応：復元可能（データベースに記録は残る）

#### 4. 削除済みレビューの復元

ソフトデリートされたレビューを復元：
- 「削除済み」タブから復元可能

---

## 技術スタック

### フロントエンド
```
React 19.x
├── react-leaflet 5.x     （地図表示）
├── Tailwind CSS 4.x      （スタイリング）
└── Vite 8.x              （ビルドツール）
```

### バックエンド
```
Laravel 13.x
├── PHP 8.3+
├── SQLite                 （開発用DB）
└── Composer              （パッケージマネージャー）
```

### 地図・外部サービス
```
Leaflet 1.9.x            （オープンソース地図ライブラリ）
├── CartoDB Dark Matter  （タイルプロバイダー）
├── Google Fonts         （日本語・英語フォント）
└── OpenStreetMap        （地図データ）
```

### デプロイメント
```
Vercel                    （フロントエンド + API ホスティング）
```

### 主な依存パッケージ

**Frontend (package.json)**：
- react, react-dom
- react-leaflet, leaflet
- tailwindcss
- vite, @vitejs/plugin-react

**Backend (composer.json)**：
- laravel/framework
- laravel/tinker
- fakerphp/faker（テスト用）

---

## セットアップガイド

### 📋 事前準備

```bash
# 推奨環境
- PHP: 8.3以上
- Node.js: 18以上
- npm: 9以上
- Composer: 最新版
- Git: インストール済み
```

### 🚀 インストール手順（5分）

#### 1. リポジトリをクローン

```bash
git clone https://github.com/your-username/cafe-finder.git
cd cafe-finder
```

#### 2. バックエンド（Laravel）のセットアップ

```bash
# PHPの依存関係をインストール
composer install

# .env ファイルを作成
cp .env.example .env

# アプリケーションキーを生成
php artisan key:generate
```

#### 3. データベースの初期化

```bash
# SQLite データベースファイルを作成
touch database/database.sqlite

# マイグレーション実行（テーブル作成）
php artisan migrate:fresh --seed

# ストレージシンボリックリンクを作成（画像アップロード用）
php artisan storage:link
```

#### 4. フロントエンド（React）のセットアップ

```bash
# Node.js の依存関係をインストール
npm install

# 開発サーバー用にビルド
npm run dev
```

#### 5. サーバーの起動

```bash
# Laravel 開発サーバーを起動（別のターミナルで実行推奨）
php artisan serve

# すると http://127.0.0.1:8000 でアクセス可能
```

#### 📦 一括セットアップスクリプト（推奨）

```bash
# composer.json に定義済み
composer run setup
```

---

### 🔧 開発モード（並行実行）

複数のプロセスを同時実行：

```bash
# 推奨：すべてのサービスを一括起動
composer run dev

# このコマンドで以下が自動起動：
# - php artisan serve （Laravelサーバー）
# - php artisan queue:listen （キュー処理）
# - php artisan pail （ログ監視）
# - npm run dev （Vite開発サーバー）
```

---

## デプロイメント

### 🌐 Vercel へのデプロイ

このプロジェクトは Vercel 対応済み：

#### 1. 前提条件
- GitHub アカウント
- Vercel アカウント（vercel.com で無料登録可能）

#### 2. デプロイ手順

**a) GitHub にプッシュ**
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

**b) Vercel にデプロイ**
```bash
# 方法1: CLI から
npm i -g vercel
vercel

# 方法2: ブラウザから
# 1. vercel.com にログイン
# 2. "Import Project" をクリック
# 3. GitHub リポジトリを選択
# 4. デプロイ設定を確認（自動検出）
# 5. "Deploy" をクリック
```

#### 3. 設定ファイル

`vercel.json` と `api/index.php` で自動設定：
- フロントエンドのビルド出力を `/public` にマッピング
- API リクエストを `api/index.php` にルーティング

#### 4. 環境変数の設定

Vercel ダッシュボードで設定：

```bash
APP_NAME=CafeFinder
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.vercel.app
DB_CONNECTION=sqlite
DB_DATABASE=/tmp/database.sqlite
```

#### 5. デプロイ完了

```
✅ デプロイ成功！
URL: https://your-domain.vercel.app
```

---

## 🔐 セキュリティ機能

### API キー管理
- ❌ **Google Maps API キーなし**：Leaflet + CartoDB を使用
- ✅ 無料で実運用可能
- ✅ API キー漏洩のリスクなし

### ファイルアップロード検証
- ファイルタイプ検証：JPEG / PNG / WebP のみ許可
- ファイルサイズ制限：1ファイル最大 3MB
- 拡張子スプーフィング検出

### データ保護
- CSRF 保護：Laravel のセッションベース実装
- SQLインジェクション対策：Eloquent ORM を使用
- XSS 対策：React の自動エスケープ

### レビュー管理
- ソフトデリート対応：削除後も復元可能
- 管理者による監視・削除機能

### 本番環境でのセキュリティ設定

⚠️ **本番環境への公開前に必ず確認してください**：

```bash
# 1. 環境変数を本番用に設定
APP_ENV=production
APP_DEBUG=false

# 2. 強力なアプリケーションキーを生成
php artisan key:generate

# 3. CORS を限定
# config/cors.php で APP_URL のみを許可

# 4. HTTPS を強制
# .env に APP_URL=https://your-domain.com

# 5. ログレベルを WARNING 以上に設定
LOG_LEVEL=warning

# 6. エラーメッセージは詳細を表示しない
# APP_DEBUG=false で自動的に隠される
```

---

## ⚡ パフォーマンス最適化

本番環境で快適に動作するため、以下の最適化が実装されています：

### 🚦 Rate Limiting（レート制限）

API に対する過度なアクセスを防ぐ：

```
- 認証系API：60リクエスト/分
- レビューAPI：100リクエスト/分
- カフェAPI：120リクエスト/分
```

**制限に達した場合**：`429 Too Many Requests` エラーが返されます

### 💾 キャッシング

以下の情報はキャッシュされ、定期的に更新されます：

| キャッシュ対象 | 有効期間 | 説明 |
|----------------|---------|------|
| レビュー集計 | 10分 | 全カフェのレビュー件数・平均評価 |
| ダッシュボード統計 | 30分 | 管理画面の統計情報 |

**キャッシュの無効化**：
- 新しいレビュー投稿時
- レビュー編集・削除時
- 新しいカフェ追加時

### 🔍 N+1 クエリの最適化

- **Eager Loading**：関連データを事前に読み込み
- **SelectRaw**：データベース側で集計計算
- **ページネーション**：管理画面は50件ずつ表示

### 🖼 画像の最適化

- **WebP対応**：最新ブラウザで自動圧縮
- **遅延読み込み**：`loading="lazy"` で必要な時だけ読み込み
- **ファイルサイズ制限**：最大 3MB

---

## 📊 本番環境での ENV 設定チェックリスト

公開前に以下をすべてチェック：

```markdown
□ APP_ENV=production に設定
□ APP_DEBUG=false に設定
□ APP_KEY が生成・更新されている
□ APP_URL が HTTPS で設定されている
□ DB_CONNECTION が本番DBに指定されている
□ LOG_LEVEL が warning 以上に設定
□ CACHE_DRIVER が redis または file に設定
□ SESSION_DRIVER が redis または database に設定
□ CORS 設定が本番ドメインに限定
□ 認証情報がセキュアに管理されている
□ .env ファイルが .gitignore に登録されている
□ SSL証明書が設定されている
□ バックアップスケジュールが設定されている
□ エラーログの保存先を確認
```

---

## 📱 ブラウザ対応

| ブラウザ | 対応状況 |
|---------|---------|
| Chrome 90+ | ✅ 完全対応 |
| Firefox 88+ | ✅ 完全対応 |
| Safari 14+ | ✅ 完全対応 |
| Edge 90+ | ✅ 完全対応 |
| iOS Safari | ✅ 完全対応 |
| Android Chrome | ✅ 完全対応 |

---

## 📊 データベーススキーマ

### テーブル構成

```sql
-- ユーザー
users (id, email, password, name, ...)

-- カフェ
cafes (id, name, hours, is_24h, wifi, outlet, 
       lat, lng, price, photos, tags, accent, specs, ...)

-- レビュー
reviews (id, user_id, cafe_id, wifi_rating, outlet_rating, 
         noise_rating, value_rating, vibe_rating, 
         crowd_level, tags, comment, created_at, deleted_at)

-- キャッシュテーブル
cache (key, value, expiration)

-- ジョブテーブル
jobs (id, queue, payload, attempts, ...)
```

---

## 🆘 トラブルシューティング

### エラー：「Vite manifest not found」

**原因**：フロントエンドのビルドが完了していない

**解決策**：
```bash
npm run build
npm run dev
```

### エラー：「No such file or directory: database.sqlite」

**原因**：データベースファイルが作成されていない

**解決策**：
```bash
touch database/database.sqlite
php artisan migrate:fresh --seed
```

### エラー：「Unauthorized」（ログイン時）

**原因**：認証情報が正しくない、またはデータベースが空

**解決策**：
```bash
# データベースをリセット（テスト環境のみ）
php artisan migrate:fresh --seed

# セットアップ時に指定した認証情報でログインしてください
```

### マップが表示されない

**原因**：Leaflet / CartoDB ライブラリが読み込まれていない

**解決策**：
```bash
npm install
npm run dev
```

---

## 📝 ライセンス

MIT License - 自由に使用、改変、配布が可能です

---

## 🤝 貢献方法

1. このリポジトリをフォーク
2. 新しいブランチを作成：`git checkout -b feature/AmazingFeature`
3. 変更をコミット：`git commit -m 'Add some AmazingFeature'`
4. ブランチにプッシュ：`git push origin feature/AmazingFeature`
5. プルリクエストを作成

---

## 📞 サポート

問題が発生した場合：

1. このドキュメント（GUIDE.md）を確認
2. トラブルシューティングセクションを確認
3. GitHub Issues で報告

---

**最終更新日：2026年5月22日**  
**バージョン：VOL.08 / FINAL EDITION**
